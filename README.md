# 🎨 Paint Online — Многопользовательский веб-редактор для рисования в реальном времени

**Paint Online** — это веб-приложение для совместного рисования на общем холсте в реальном времени. Поддерживает множество инструментов, стилей линий, отмену действий, сохранение изображений и синхронизацию между всеми подключёнными пользователями.

---

## 🚀 **Основные возможности**

- **Рисование в реальном времени** с синхронизацией между всеми участниками
- **Инструменты:**
  - Кисть
  - Прямоугольник
  - Круг / Эллипс
  - Линия
  - Треугольник
  - Ластик
- **Настройка стилей:**
  - Толщина линии (0–60)
  - Тип линии (сплошная, пунктир, разные шаблоны)
  - Цвет обводки и заливки
- **История действий:**
  - Отмена (Undo)
  - Повтор (Redo)
- **Совместная работа:**
  - Генерация уникальной ссылки на холст
  - Возможность копирования ссылки
- **Сохранение и загрузка:**
  - Скачивание изображения
  - Автосохранение на сервере
  - Восстановление холста после перезагрузки
- **Визуальный интерфейс:**
  - Адаптивный дизайн
  - Интуитивная панель инструментов и настроек
  - Прелоадер загрузки

---

## 🛠 **Технологии**

### **Frontend (Client):**
- **React** + **React Router** для маршрутизации
- **MobX** для управления состоянием
- **Axios** для HTTP-запросов
- **WebSocket** для реального взаимодействия
- **SCSS** для стилей с использованием переменных
- **HTML5 Canvas** для отрисовки графики
- **React Hooks** (useState, useEffect, useRef)

### **Backend (Server):**
- **Node.js** + **Express**
- **WebSocket** через `express-ws`
- **CORS** для разрешения кросс-доменных запросов
- **FS** для работы с файловой системой (сохранение изображений)
- **Body-parser** для обработки JSON

---

## 📁 **Структура проекта**

```
paint-online/
├── client/                 # React-приложение
│   ├── public/
│   └── src/
│       ├── components/     # Компоненты интерфейса
│       │   ├── App/
│       │   ├── Canvas/
│       │   ├── ToolBar/
│       │   ├── SettingBar/
│       │   ├── Loader/
│       │   └── CopyLink/
│       ├── store/          # MobX-сторы (canvasState, toolState, loaderState)
│       ├── tools/          # Классы инструментов (Brush, Rect, Circle, Line и т.д.)
│       ├── api/            # HTTP-запросы (postImage, getImage)
│       ├── socket/         # WebSocket-логика
│       └── utils/          # SCSS-переменные и стили
└── server/                 # Express + WebSocket сервер
    ├── files/              # Папка для сохранения изображений
    └── index.js            # Основной серверный файл
```

---

## 🧩 **Особенности реализации**

1. **WebSocket-синхронизация:**  
   Все действия (рисование, очистка, undo/redo) транслируются через WebSocket и применяются у всех подключённых пользователей.

2. **Сохранение состояния холста:**  
   Изображение сохраняется на сервере в формате Base64 и восстанавливается при загрузке страницы.

3. **Гибкая система инструментов:**  
   Каждый инструмент реализован как отдельный класс, наследуемый от базового `Tools`. Это позволяет легко добавлять новые инструменты.

4. **История изменений (undo/redo):**  
   Реализована через стеки `undoList` и `redoList` с сохранением снимков состояния canvas.

5. **Адаптивный интерфейс:**  
   Используются SCSS-модули и CSS-анимации (например, для прелоадера).

---

## ⚙️ **Установка и запуск**

### **1. Клонирование репозитория**
```bash
git clone <repository-url>
cd paint-online
```

### **2. Запуск сервера**
```bash
cd server
npm install
npm start
```
Сервер запустится на `http://localhost:5000`

### **3. Запуск клиента**
```bash
cd ../client
npm install
npm start
```
Клиент запустится на `http://localhost:3000`

---

## 🌐 **Пример использования**

1. Откройте приложение в браузере.
2. Выберите инструмент (кисть, фигура и т.д.).
3. Настройте толщину, тип линии и цвета.
4. Начните рисовать — изменения будут видны всем, у кого открыта та же ссылка.
5. Скопируйте ссылку из блока «Ссылка на холст» и отправьте другому пользователю для совместного рисования.

---

## 👨‍💻 **Автор**

Павел Кулебин